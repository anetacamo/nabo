import Button from './button.type';

type SectionType = {
  title?: string;
  text?: string;
  background?: string;
  orientation?: string;
  buttons?: Button[];
  button?: Button;
};

export default SectionType;
